<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 悬浮窗权限 -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- 无障碍服务权限 -->
    <uses-permission android:name="android.permission.BIND_ACCESSIBILITY_SERVICE" />
    <!-- 接收启动完成广播 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- 唤醒锁权限，用于保活 -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- 前台服务权限 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- 请求忽略电池优化，用于改善后台运行稳定性 -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- 网络权限，用于上报设备信息 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 网络状态权限 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Android 9 及以下将图片保存到相册 -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <!-- 复述题：本地语音识别所需的录音权限（运行时危险权限） -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- 复述题：若后续采用独立麦克风前台服务，面向 Android 14+ 时评估以下两项 -->
    <!--
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    -->

    <!-- Android 11+ 包可见性：用于在“添加 APP”时列出已安装的可启动应用 -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Basic"
        tools:targetApi="31">
        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 复述题录音 Activity：透明主题，录音时本应用处于前台以稳定取得麦克风"使用期间"权限 -->
        <activity
            android:name=".challenge.retelling.RetellingRecordActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:theme="@style/Theme.Retelling.Transparent"
            android:taskAffinity="" />

        <!-- 临时调试工具：内置音频不同长度识别测试 -->
        <activity
            android:name=".ui.AsrTestActivity"
            android:exported="false" />

        <!-- 无障碍服务 (已集成悬浮窗管理) -->
        <service
            android:name=".floating.FloatService"
            android:exported="false"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>
    </application>

</manifest>
